#include "Testing.hpp"

bool RunTestCases() {
    //Test Cases are now handled in the python framework.
    //TODO: There are probably some other tests we can run here. Write some at some point :)
    return true;
}
